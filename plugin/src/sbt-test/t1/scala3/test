> compile

> projects
> show baseDirectory

> show unusedCode
> check1
> scalafixAll WarnUnusedCode
-> scalafixAll ErrorUnusedCode

> set ThisBuild / unusedCodeConfig ~= (_.copy(excludeEnumCase = true))
> unusedCode
> check2
> reload

> scalafixAll RemoveUnusedCode

> a1/runMain hello.x1
> a1/runMain hello.x2
